#include <bits/stdc++.h>
#define MAX 100
#define oo 1e18
using namespace std;
int W[MAX][MAX],D[MAX][MAX];
int main(){
  string s,t;
  cin >> s >> t;
  int n;
  cin >> n;
  for(int i = 0; i < n; i++){
    char a,b;
    int w;
    cin >> a >> b >> w;
  }
  if(s.size() != t.size()){
    cout << -1 << endl;
  }
}
